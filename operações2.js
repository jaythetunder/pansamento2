// contador negativo

let contador = 100;
contador--;

console.log("O resultado do decremento é: " + contador);

// interpolação

const nome = "Yudi"
console.log(`Olá, ${nome}! Bem vindo ao nosso sistema.`);

const a = 5;
const b = 3; 

console.log (`A soma de ${a} + ${b} é ${a+b}.`);

// interpolação dentro de um objeto

const usuario = (
   nome: "Cleiton Oswaldo Robert de Cruz",
   idade: 30;
   cidade: "Rio de janeiro"
)
console.log(" O cliente ${}   ")